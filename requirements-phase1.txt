# Direct training dependencies for the isolated phase-one experiment (Python 3.11).
# Data/audit/benchmark-freeze stages use only the Python standard library.
torch==2.8.0
transformers==4.55.4
peft==0.17.1
datasets==4.0.0
accelerate==1.10.1
huggingface-hub==0.34.4
safetensors==0.6.2
tokenizers==0.21.4
numpy==2.2.6
